import {initcode} from '../shared/ide';
import {CoordinateSystem, EquationSystem, Gesture, Slider, Slideshow, Step, Tabbox} from '../shared/types';


export function parametersandarguments($step: Step) {
    initcode($step);
}

export function noneitsnothing($step: Step) {
    initcode($step);
}

export function defaultreturn($step: Step) {
    initcode($step);
}

export function defaultarguments($step: Step) {
    initcode($step);
}

export function usingkeywordandpositionalarguments($step: Step) {
    initcode($step);
}

export function keywordarguments($step: Step) {
    initcode($step);
}

export function dontusemutabledefaultarguments($step: Step) {
    initcode($step);
}

export function usingnoneasasentinel($step: Step) {
    initcode($step);
}

export function unpackingmultiplereturns($step: Step) {
    initcode($step);
}

export function positionalargumentunpacking($step: Step) {
    initcode($step);
}

export function keywordargumentunpacking($step: Step) {
    initcode($step);
}

export function usingbothkeywordandpositionalunpacking($step: Step) {
    initcode($step);
}

export function passingcontainersasarguments($step: Step) {
    initcode($step);
}

export function thenile($step: Step) {
    initcode($step);
}